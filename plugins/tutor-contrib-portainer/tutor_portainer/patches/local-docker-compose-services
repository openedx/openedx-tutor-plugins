portainer:
  image: {{ PORTAINER_DOCKER_IMAGE }}
  restart: unless-stopped
  command: --bind=:{{ PORTAINER_PORT }}
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - ../../data/portainer:/data
